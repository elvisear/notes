import { Box } from "@chakra-ui/react";
import { MagnifyingGlass } from "phosphor-react";

export default function DrawerSearch() {
  return (
    <Box mt={2}>
      <MagnifyingGlass className="icon-small" />
    </Box>
  );
}
